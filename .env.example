# 配置优先级: config.yaml > 环境变量 > 默认值
# 如需使用 YAML 配置，请复制 config.yaml.example 为 config.yaml

# DeepSeek API 配置 (必需)
DEEPSEEK_API_KEY=your-api-key-here

# API 地址 (可选，默认为 DeepSeek 官方地址)
DEEPSEEK_BASE_URL=https://api.deepseek.com

# 模型名称 (可选，默认为 deepseek-chat)
DEEPSEEK_MODEL=deepseek-chat

# 运行环境: dev/prod/test (默认 prod)
# dev: 开启热重载，日志级别默认 DEBUG
# prod/test: 关闭热重载，日志级别默认 INFO
ENV=dev

# 服务配置 (可选)
PORT=8000
# 日志级别 (可选，不设置则根据 ENV 自动选择)
# LOG_LEVEL=INFO
